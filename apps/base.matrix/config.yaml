
# App Information for Store
name: monitor
version: 1
configVersion: 2
description: Matrix stats

dataTypes:
  - device
  - monitor

# screens are comprised of widgets
screens:
  # Row 1
  - - info
  # Row 2
  - - cpuChart
  # Row 3
  - - cpu
    - memory

widgets:
  info:
    type: device
    display: list-group
  cpuChart:
    type: monitor
    data: cpu
    display: line
    label: CPU Chart
  cpu:
    type: monitor
    data: cpu
    display: digit
    format: round
  memory:
    type: monitor
    data: memory
    display: digit
    format: round
