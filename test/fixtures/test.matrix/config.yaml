name: test
dataTypes:
  test:
    foo: integer
description: none
configVersion: 2
keywords: test

sensors:
  - temperature

integrations:
  - gpio

events:
  - global

services:
  faceSvc:
    engine: detection
    type: face
  recogSvc:
    engine: recognition
    type: face
