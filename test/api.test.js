var fs = require('fs-extra');
var Matrix = require('../app').Matrix;
var should = require('should');



describe('Matrix<->API', function() {
  this.timeout(15000)
  it('can recieve a token');
  it('can regenerate a token');
  it('can send data points to API');
  it('can check for updates');
  it('can start a heartbeat');
});
